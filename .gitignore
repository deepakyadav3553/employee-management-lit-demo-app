# Dependencies
/node_modules/

# Build output
# `npm run build` (tsc) emits .js / .d.ts / .js.map mirroring each src/
# subdirectory into the repo root. Ignore those generated directories and
# artifacts so only the source under src/ is committed.
/components/
/types.js
*.js.map
*.d.ts
*.tsbuildinfo

# Claude Code / local tooling
.claude/

# Editor / OS files
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
